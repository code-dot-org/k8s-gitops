apiVersion: kargo.akuity.io/v1alpha1
kind: ClusterConfig
metadata:
  name: cluster
spec:
  webhookReceivers:
    - name: github-org-webhook
      github:
        secretRef:
          name: github-org-webhook-secret
