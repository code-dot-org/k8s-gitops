apiVersion: apps/v1
kind: Deployment
metadata:
  name: cdo-dashboard
spec:
  template:
    spec:
      containers:
        - name: dashboard
          image: code-dot-org:replace-me
          env:
            - name: RAILS_ENV
              value: levelbuilder
            - name: RACK_ENV
              value: levelbuilder
