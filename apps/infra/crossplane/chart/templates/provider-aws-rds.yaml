{{- if .Values.provider.aws.rds.enabled -}}
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: "provider-aws-rds"
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  package: "xpkg.upbound.io/upbound/provider-aws-rds:v2.5.1"
  runtimeConfigRef:
    name: "aws-runtime-config"
{{- end }}
