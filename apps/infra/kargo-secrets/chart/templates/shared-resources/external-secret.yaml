{{- $config := required "codeai_cluster_config is required" (index .Values "codeai_cluster_config") -}}
{{- $clusterName := .Values.sharedResources.clusterName | default (required "codeai_cluster_config.cluster_name is required" (index $config "cluster_name")) -}}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kargo-k8s-gitops
  namespace: {{ .Values.sharedResources.namespace }}
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: aws-secrets-manager-store-kargo-shared-resources
    kind: SecretStore
  target:
    name: kargo-k8s-gitops
    creationPolicy: Owner
    template:
      engineVersion: v2
      metadata:
        labels:
          kargo.akuity.io/cred-type: git
      data:
        repoURL: https://github.com/code-dot-org/k8s-gitops.git
        username: "{{ `{{ .username }}` }}"
        password: "{{ `{{ .password }}` }}"
  data:
    - secretKey: username
      remoteRef:
        key: {{ printf "k8s/tofu/%s/kargo/gitops_repo_username" $clusterName | quote }}
    - secretKey: password
      remoteRef:
        key: {{ printf "k8s/tofu/%s/kargo/gitops_repo_password" $clusterName | quote }}
