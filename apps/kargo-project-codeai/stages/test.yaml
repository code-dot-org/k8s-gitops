apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: test
  namespace: kargo-project-codeai
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: kargo-project-codeai
      sources:
        stages:
          - staging
  verification:
    analysisTemplates:
      - name: codeai-test-rollout
    args:
      - name: namespace
        value: test
      - name: deployment
        value: test-cdo-dashboard
      - name: expected_image
        value: ${{ imageFrom("ghcr.io/code-dot-org/code-dot-org").RepoURL }}:${{ imageFrom("ghcr.io/code-dot-org/code-dot-org").Tag }}
  promotionTemplate:
    spec:
      vars:
        - name: gitopsRepo
          value: https://github.com/code-dot-org/k8s-gitops.git
        - name: sourceRepo
          value: https://github.com/code-dot-org/code-dot-org.git
        - name: imageRepo
          value: ghcr.io/code-dot-org/code-dot-org
        - name: legacyEnv
          value: test
        - name: deployment
          value: test
        - name: envType
          value: test
        - name: namespace
          value: test
        - name: releaseName
          value: test
        - name: targetBranch
          value: stage/test
        - name: renderPath
          value: ./out/apps/codeai/deployments/test/deploy/manifests.yaml
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitopsRepo }}
            checkout:
              - branch: main
                path: ./meta
                sparse:
                  - apps/codeai
              - branch: main
                path: ./gate
                sparse:
                  - warehouses/codeai/legacy-gitflow/test/merged
              - branch: ${{ vars.targetBranch }}
                create: true
                path: ./out
        - uses: yaml-parse
          as: legacy-gate
          config:
            path: ./gate/warehouses/codeai/legacy-gitflow/${{ vars.legacyEnv }}/merged/${{ imageFrom(vars.imageRepo).Tag }}.yaml
            outputs:
              - name: revision
                fromExpression: revision
              - name: tag
                fromExpression: tag
              - name: mergedAt
                fromExpression: mergedAt
        - uses: git-clone
          config:
            repoURL: ${{ vars.sourceRepo }}
            checkout:
              - commit: ${{ commitFrom(vars.sourceRepo).ID }}
                path: ./src
                sparse:
                  - k8s/helm
        - uses: git-clear
          config:
            path: ./out
        - uses: yaml-update
          config:
            path: ./meta/apps/codeai/deployments/${{ vars.deployment }}/values.yaml
            updates:
              - key: image
                value: ${{ imageFrom(vars.imageRepo).RepoURL }}:${{ imageFrom(vars.imageRepo).Tag }}
        - uses: helm-template
          config:
            path: ./src/k8s/helm
            releaseName: ${{ vars.releaseName }}
            namespace: ${{ vars.namespace }}
            valuesFiles:
              - ./meta/apps/codeai/envTypes/${{ vars.envType }}.values.yaml
              - ./meta/apps/codeai/deployments/${{ vars.deployment }}/values.yaml
            outPath: ${{ vars.renderPath }}
        - uses: git-commit
          as: commit
          config:
            path: ./out
            message: |
              Render test for ${{ commitFrom(vars.sourceRepo).ID }} with ${{ imageFrom(vars.imageRepo).Tag }}
        - uses: git-push
          config:
            path: ./out
            targetBranch: ${{ vars.targetBranch }}
        - uses: argocd-update
          config:
            apps:
              - name: codeai-test
                sources:
                  - repoURL: ${{ vars.gitopsRepo }}
                    desiredRevision: ${{ outputs.commit.commit }}
