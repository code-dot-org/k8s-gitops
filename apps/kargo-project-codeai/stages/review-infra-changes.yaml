apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: review-infra-changes
  namespace: kargo-project-codeai
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: codeai-builds
      sources:
        stages:
          - test
  promotionTemplate:
    spec:
      vars:
        - name: gitopsRepo
          value: https://github.com/code-dot-org/k8s-gitops.git
        - name: legacyEnv
          value: production
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitopsRepo }}
            checkout:
              - commit: ${{ commitFrom('https://github.com/code-dot-org/k8s-gitops.git', warehouse('codeai-builds').ID) }}
                path: ./freight
              - branch: main
                path: ./gate
              - branch: main
                path: ./gitops
        - uses: yaml-parse
          as: build-lock
          config:
            path: ./freight/warehouses/codeai/builds/current.yaml
            outputs:
              - name: releaseId
                fromExpression: releaseId
              - name: gitCommit
                fromExpression: gitCommit
              - name: packagingKind
                fromExpression: packaging.kind
              - name: sourcePath
                fromExpression: packaging.sourcePath
        - uses: yaml-parse
          as: legacy-merge
          config:
            path: ./gate/warehouses/codeai/legacy-gitflow/${{ vars.legacyEnv }}/merged/${{ outputs['build-lock'].releaseId }}.yaml
            outputs:
              - name: revision
                fromExpression: revision
              - name: tag
                fromExpression: tag
              - name: mergedAt
                fromExpression: mergedAt
        - uses: yaml-parse
          as: deployment-meta
          config:
            path: ./gitops/apps/codeai/deployments/production/deployment.yaml
            outputs:
              - name: envType
                fromExpression: envType
              - name: namespace
                fromExpression: namespace
        - uses: yaml-update
          config:
            path: ./gitops/apps/codeai/deployments/production/deploy/kustomization.yaml
            updates:
              - key: namespace
                value: ${{ outputs['deployment-meta'].namespace }}
              - key: resources
                value:
                  - github.com/code-dot-org/code-dot-org//k8s/kustomize/base?ref=${{ outputs['build-lock'].gitCommit }}
              - key: components
                value:
                  - ../../envTypes/${{ outputs['deployment-meta'].envType }}
        - uses: kustomize-set-image
          config:
            path: ./gitops/apps/codeai/deployments/production/deploy
            images:
              - image: code-dot-org
                newName: ghcr.io/code-dot-org/code-dot-org
                tag: ${{ outputs['build-lock'].releaseId }}
        - uses: git-commit
          config:
            path: ./gitops
            message: Review production deploy update for ${{ outputs['build-lock'].releaseId }} [skip ci]
        - uses: git-push
          as: push
          config:
            path: ./gitops
            generateTargetBranch: true
        - uses: git-open-pr
          config:
            repoURL: https://github.com/code-dot-org/k8s-gitops.git
            sourceBranch: ${{ outputs.push.branch }}
            targetBranch: main
            title: Review CodeAI production deploy for ${{ outputs['build-lock'].releaseId }}
