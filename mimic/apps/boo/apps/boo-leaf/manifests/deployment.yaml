apiVersion: apps/v1
kind: Deployment
metadata:
  name: boo-leaf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boo-leaf
  template:
    metadata:
      labels:
        app: boo-leaf
    spec:
      containers:
        - name: echo
          image: hashicorp/http-echo:1.0.0
          args:
            - -text=boo leaf
          ports:
            - containerPort: 5678
