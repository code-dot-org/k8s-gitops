These files are legacy gitflow coexistence gates, not Freight inputs.
GitHub Actions in `code-dot-org` update `current.yaml` and `merged/git-<full-commit-sha>.yaml`
when the legacy `staging`, `test`, `levelbuilder`, and `production` branches advance.
